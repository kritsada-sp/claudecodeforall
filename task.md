# Task List: ปรับจำนวนเมนูอาหารไทยเป็น 12 เมนู

อ้างอิงจาก `CONTEXT.md` — คำว่า **Dish** (เมนูหนึ่งรายการในลิสต์), **Dish of the Day** (เมนูที่สุ่ม/ไฮไลต์ประจำวัน), **Favorites** (ชุดเมนูที่ผู้ใช้ถูกใจ) ยังคงนิยามเดิม การเปลี่ยนแปลงนี้เป็นแค่ขนาดชุดข้อมูล ไม่กระทบตัวโดเมนโมเดล

- [x] **เลือก 2 เมนูที่จะตัดออก**: ตัด **ผัดซีอิ๊ว** (ซ้ำแนวกับผัดไทย/ข้าวผัด) และ **ต้มยำทะเล** (ซ้ำแนวกับต้มยำกุ้ง) เหลือ 12 เมนู
- [x] แก้ไข `DISHES` array ใน `index.html` ให้เหลือ 12 รายการตามที่เลือก
- [x] ตรวจสอบว่าปุ่มสุ่ม (reroll) และกลไก Dish of the Day ยังทำงานถูกต้องกับชุดเมนูใหม่ 12 รายการ (ไม่สุ่มซ้ำเมนูก่อนหน้า)
- [x] ตรวจสอบเคส stale data: จำลอง `localStorage` เดิมที่อ้างอิงเมนูที่ถูกตัดออก (เป็นทั้ง favorite และ Dish of the Day) — ระบบ fallback ไปสุ่มเมนูใหม่ที่ยังมีอยู่จริงได้ถูกต้อง และเพิ่ม logic กรอง/บันทึกทับ favorite ที่อ้างอิงเมนูที่ไม่มีอยู่แล้วออกจาก `localStorage` ทันทีตอนโหลดหน้า
- [x] ทดสอบเว็บในเบราว์เซอร์อีกครั้งหลังแก้ไข: เปิดหน้าเว็บ, กดปุ่มสุ่ม, กดปุ่มถูกใจ, ตรวจสอบเมนูโปรด — ผ่านทั้งหมด
